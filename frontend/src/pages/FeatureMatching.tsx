// Feature Matching page — Task 2
import React from "react";

const FeatureMatching: React.FC = () => {
  return (
    <div>
      <h1>Feature Matching</h1>
      <p>SIFT descriptor extraction + Lowe's ratio sweep (0.60 / 0.75 / 0.90).</p>
    </div>
  );
};

export default FeatureMatching;
